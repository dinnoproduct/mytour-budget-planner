# Production Build

Run the production build for this project.

```bash
npm run build
```

Execute the webpack production build. Stream the output. When complete, report whether the build succeeded or failed. If it failed, summarize the errors so the user knows what to fix.
