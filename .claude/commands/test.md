# Run Tests

Run the Vitest test suite.

```bash
npm run test
```

Execute all tests in node environment. Report the number of passed, failed, and skipped tests. If any tests fail, show the failure details so the user knows what broke.
