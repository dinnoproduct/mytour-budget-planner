# Run Linter

Run ESLint across the project.

```bash
npm run lint
```

Execute the linter. If there are errors or warnings, list them grouped by file. If the lint passes cleanly, confirm that to the user. Do not auto-fix unless the user explicitly asks.
