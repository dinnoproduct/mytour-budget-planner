# Start Dev Server

Run the development server for this project.

```bash
npm run dev
```

Start the webpack dev server on port 5173. Stream the output so the user can see when the server is ready. Inform the user the app is available at `http://localhost:5173`.
